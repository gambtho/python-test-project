Nearly empty python project with a failing test!
===

To setup the environment:

```
$ source ./start
```

To run the (failing) test:

```
$ python -m unittest
```

To tear down the environment

```
$ deactivate
```